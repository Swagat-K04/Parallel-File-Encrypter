{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "make",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}" // Ensure it's the correct folder
            },
            "problemMatcher": [
                "$gcc" // Matches GCC output for errors
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
  }
  